version: '3.9'

services:
  eduweconnect-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduweconnect-frontend
    ports:
      - "7005:7000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
